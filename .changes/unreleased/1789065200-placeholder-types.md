---
type: Added
title: Custom placeholder types
doc: placeholder-types
---

Mask a value under a name of your own instead of the kind a detector guessed: every kind picker ends in **Custom type…**, so a judge becomes `JUDGE1` rather than `NAME1` and the language model keeps the context. Settings → Masking keeps the types you use often, each one global or bound to the workspace you added it in.
