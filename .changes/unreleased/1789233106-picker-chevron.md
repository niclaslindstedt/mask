---
type: Fixed
title: Kind pickers keep their chevron on the value's row
---

The kind picker in the review list, on the Variables tab and in Settings → Masking dropped its chevron onto a line of its own below the value on a narrow screen, stretching every row; the row layout is now part of the trigger rather than something a caller can style away.
